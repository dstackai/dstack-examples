workflows:
  - name: train-tensorboard
    provider: bash
    ports:
      - 6006
    commands:
      - pip install torchvision pytorch-lightning tensorboard
      - tensorboard --port 6006 --host 0.0.0.0 --logdir ./lightning_logs &
      - python tensorboard/train.py
    artifacts:
      - path: ./lightning_logs